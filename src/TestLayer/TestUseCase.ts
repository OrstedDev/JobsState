import { TestAwait } from "./TestAwait";
import { Test } from "./Test";

const TestUseCase = async (): Promise<void> => {
  //console.clear();
  // Test();
  // TestAwait();
};

export default TestUseCase;
